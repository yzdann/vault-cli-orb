description:
  Example of using this orb to install vault cli.

usage:
  version: 2.1
  orbs:
    vault-cli: yzdann/vault-cli-orb@1.2.3
  workflows:
    use-my-orb:
      jobs:
        - vault-cli/vault-install
